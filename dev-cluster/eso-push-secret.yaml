apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: dev-cluster-vault-sync
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: dev-cluster-app
  data:
    - match:
        secretKey: password
        remoteRef:
          remoteKey: "dev-cluster/database"
          property: password
    - match:
        secretKey: user
        remoteRef:
          remoteKey: "dev-cluster/database"
          property: username
    - match:
        secretKey: dbname
        remoteRef:
          remoteKey: "dev-cluster/database"
          property: dbname
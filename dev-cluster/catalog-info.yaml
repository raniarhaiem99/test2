apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: dev-cluster
  description: "Postgres cluster"
  annotations:
    argocd/app-name: dev-cluster
    # Points the Vault tab directly to this cluster's dossier
    vault.io/secrets-path: "clusters/dev-cluster"
    vault.io/secrets-engine: "secret"
    vault.io/kv-version: "2"
spec:
  type: database
  owner: guest
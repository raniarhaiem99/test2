apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: test-cluster-5-vault-sync
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: test-cluster-5-app
  data:
    - match:
        secretKey: password
        remoteRef:
          # This creates: kv/clusters/test-cluster/password
          remoteKey: "clusters/test-cluster-5"
          property: password
    - match:
        secretKey: user
        remoteRef:
          # This creates: kv/clusters/test-cluster/username
          remoteKey: "clusters/test-cluster-5"
          property: username
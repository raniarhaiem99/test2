apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: arania-vault-sync
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: arania-app
  data:
    - match:
        secretKey: password
        remoteRef:
          remoteKey: "cluster-postgres-arania/database"
          property: password
    - match:
        secretKey: user
        remoteRef:
          remoteKey: "cluster-postgres-arania/database"
          property: username
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: cluster-postgres-vault-sync
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: cluster-postgres-app
  data:
    - match:
        secretKey: password
        remoteRef:
          # This creates: kv/clusters/test-cluster/password
          remoteKey: "clusters/cluster-postgres"
          property: password
    - match:
        secretKey: user
        remoteRef:
          # This creates: kv/clusters/test-cluster/username
          remoteKey: "clusters/cluster-postgres"
          property: username